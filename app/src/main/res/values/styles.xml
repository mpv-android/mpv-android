<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="MaterialYou" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryContainer">@color/primary</item>
        <item name="colorAccent">?colorPrimaryContainer</item>
        <!--     WTF   https://stackoverflow.com/a/47242474/16867144   -->
        <item name="android:colorAccent">?colorPrimaryContainer</item>

        <item name="editTextStyle">@style/Widget.Material3.TextInputEditText.OutlinedBox</item>
        <item name="materialCardViewStyle">@style/Widget.Material3.CardView.Elevated</item>
    </style>


    <style name="Player" parent="@style/MaterialYou">
        <item name="android:windowFullscreen">true</item>
        <item name="android:navigationBarColor">@color/player_osd_background</item>
        <item name="android:statusBarColor">@color/player_osd_background</item>
        <item name="tint">@color/player_icon_state_tint</item>
        <item name="android:textColor">@android:color/white</item>
        <!--        Toolbar icon color-->
        <item name="colorControlNormal">@android:color/white</item>
        <item name="android:actionMenuTextColor">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="titleTextColor">@android:color/white</item>
        <item name="android:windowBackground">#000000</item>
        <!--        Ignore notch inset-->
        <item name="android:windowLayoutInDisplayCutoutMode" tools:ignore="NewApi">shortEdges</item>
    </style>

    <style name="Player.IconButton">
        <item name="android:background">@drawable/rounded_image_button_bg</item>
        <item name="android:padding">12dp</item>
        <item name="android:layout_marginStart">4dp</item>
        <item name="android:layout_marginEnd">4dp</item>
    </style>

    <style name="MaterialYou.Preference" parent="@style/MaterialYou">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
        <item name="toolbarStyle">@style/MaterialYou.Preference.Toolbar</item>
        <item name="alertDialogTheme">@style/Theme.Material3.DayNight.Dialog.Alert</item>
        <item name="dialogCornerRadius">28dp</item>
    </style>

    <style name="MaterialYou.Preference.Toolbar" parent="Widget.Material3.Toolbar">
        <item name="android:background">?colorSurface</item>
    </style>

    <style name="Preference.SwitchPreferenceCompat" parent="Preference.SwitchPreferenceCompat.Material" tools:override="true">
        <item name="android:widgetLayout">@layout/material_switch</item>
    </style>

    <!--    ________________________________   -->

    <style name="FilePickerTheme" parent="Theme.AppCompat">
        <item name="nnf_separator_color">@color/primary</item>

        <!-- Set these to match your theme -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/primary</item>

        <!-- Need to set this also to style create folder dialog -->
        <item name="alertDialogTheme">@style/FilePickerTheme.AlertDialog</item>
        <item name="android:textColorPrimary">@android:color/background_light</item>
        <item name="android:textColorLink">@android:color/background_light</item>
    </style>

    <style name="FilePickerThemeSpecial" parent="FilePickerTheme">
        <!-- used to draw behind navigation bar -->
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="FilePickerTheme.AlertDialog" parent="Theme.AppCompat.Dialog.Alert">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
    </style>

    <!-- duplicated for the sake of PreferenceActivity, which isn't AppCompat -->
    <style name="FilePickerThemeLegacy" parent="@android:style/Theme.Material">
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorPrimaryDark">@color/primary</item>
        <item name="android:colorAccent">@color/primary</item>
        <item name="android:textColorPrimary">@android:color/background_light</item>
        <item name="android:textColorLink">@android:color/background_light</item>

        <item name="android:alertDialogTheme">@style/FilePickerThemeLegacy.AlertDialog</item>
    </style>

    <style name="FilePickerThemeLegacy.AlertDialog" parent="@android:style/Theme.Material.Dialog.Alert">
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorPrimaryDark">@color/primary_dark</item>
    </style>

</resources>
